<?xml version="1.0" encoding="utf-8"?>
<!-- dev-lint: android-webview allow=application/@android:networkSecurityConfig
     (held identical to dev-lint's canonical WebView manifest, but for the anchor
     below — this is the only app in the fleet that speaks to a privately issued
     certificate, and without the anchor every connection to it begins as an
     error the network stack resolves without asking us: measured at one failed
     handshake per five-second poll. Declared here rather than added to the
     canonical manifest, because no other wrapper has anything to anchor.) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The prompt that makes the secure-element key usable again once its
         authentication window has run out. See Unlock.kt. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Console">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
