# #89 — A message being written should survive leaving the chat

Reported 2026-08-06. Typed words and a chosen picture are lost on leaving a session and coming back. They should still be there.

Both halves: the text in the composer and the picture already picked and scaled — the picture especially, since it cost a scale and an upload over a phone connection, which is the same reason send() already keeps it when a send fails.

Per session, not global: two conversations each hold their own unsent message.
