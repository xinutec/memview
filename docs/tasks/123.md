# #123 — Nix-build the console, but keep the plist pointing at a stable symlink

The fleet's rule is that a launchd agent runs a store path, never `~/Code/...`
(`reference_mac_agents_run_from_store`). The console is the exception that proves
why the rule needs a qualifier.

## The trap

Put a store path in `ProgramArguments` and every console change moves it, so the
plist changes — and applying that costs every open session, because the restart is
unload/load and **SIGTERM is the console's deliberate stop path**.

⚠ **Measured 2026-08-08, correcting the first version of this note:** home-manager
does NOT reload a changed agent by itself. After `switch.sh` the plist on disk had
the new value while the running job still had the old one, `runs = 1`, same pid.
So the reload is manual (`launchctl bootout` + `bootstrap`) and the cost is paid
deliberately rather than by surprise — better than feared, but the cost is real.

⚠ **And SIGUSR2 cannot substitute.** `handover` re-execs with the process's
existing environment, so it upgrades the BINARY and never the plist. Anything in
`EnvironmentVariables` — PATH, `CONSOLE_DIRS`, the gate — needs a real restart,
which is the one operation that ends every session. Worth knowing before changing
the plist while sessions are live.

## The shape that works

- Build with nix; install the result behind a **stable symlink**
  (`~/.local/libexec/agent-console`). The plist never changes, so home-manager
  never reloads the agent.
- `handover()` must exec that symlink, not `std::env::current_exe()` — Rust
  canonicalises, which would resolve back to the old immutable store path and make
  SIGUSR2 a silent no-op that looks like it worked. One-line change: prefer
  `CONSOLE_BINARY` when set.
- The **frontend is the risky half**, not the Rust. `build:console` is `ng build`
  plus rsync, and esbuild's macOS teardown abort can land before `index.html`
  flushes — thoth shipped a 0-byte index.html and crash-looped its agent 33 times.
  Never accept `test -d dist/…`; end the build in a bundle validation whose exit
  code is the check.

## Not first

Eight open tasks are console bugs (#106, #111, #112, #115, #116, #120, #121,
#122). At cargo speed a fix is `cargo build` + SIGUSR2, seconds, sessions intact;
pinned it is edit → commit → `switch.sh`. Do this once that churn settles.
