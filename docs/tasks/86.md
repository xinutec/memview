# #86 — One action should draw one widget, not two

Reported 2026-08-06. With the session on manual — and, from the report, on Accept Edits too — every action puts two widgets on the screen where there should be one.

Not investigated yet and deliberately not diagnosed here: find out first whether it is one event rendered twice, two events for one action, or a card that is re-keyed mid-flight. #79 is the cautionary note — the question card's id-keyed state is deliberate, and the defect there turned out to be the tracking rather than the card.

Same family as the two just fixed (a message with two text blocks drawn as two prompts, a compact summary drawn as nothing): the console saying something about what happened that is not what happened.
