# #92 — Abstractly evaluate what the agents run, across languages, into one effect language

One effect language for what the agents run, across shell/Python/nested shells, evaluated abstractly rather than executed.

Reader stands at **99.0% of 648,142 simple commands** (2026-08-06, 134,004 Bash calls / 1,205 transcripts). Unread 6,775.

Landed: e93c140 a name bound to a literal; 7dbe99a a partly-known value still names its command ($ADB 1,071 unread → 0); c8e2242 a cd nobody can follow poisons its scope's directory rather than leaving the old one in force.

Two structural limitations remain, both in README's roadmap:
1. No loop is unrolled — a loop over a literal list, or `seq` with constant bounds, names nothing.
2. An undetermined subject vanishes instead of counting, so the record looks more complete than it is.

Below those the tail is verb-table rows, not shapes: dhall-to-json 334, k3s 284, screen 244, journalctl 203, probe 201. Most name no files, so a row buys little.

Standing decisions: external scripts on disk stay unparsed (user's call — not reconstructable); regexes unparsed for now; SQL reader not worth building (44 heredoc bodies, 22 kB).
